export function GET() { return new Response('# Empirical Observatory\n\nDeclared-model scientific computation with explicit premises, units, order and unresolved gaps.\n\n- Human desk: /question\n- AI documentation: /agents\n- Capability catalogue and complete JSON examples: GET /api/inquiry\n- Optional shared AI interpreter: GET /api/inquiry/interpret for readiness; POST {prompt} for a proposal or missing inputs. Inspect before execution. Shared daily call limit applies.\n- OpenAPI: /api/inquiry/openapi\n- Compute: POST /api/inquiry, application/json, exactly {prompt} or {inquiry, previous?}\n- Optional private storage: POST /api/inquiry/save with {request}; read/delete /api/inquiry/ID with Bearer token\n- Source-pinned encyclopedia: /encyclopedia\n- Entry index: /research-index/entries.json\n\nNo LLM key required. Finite declared models only. Do not describe model outputs as empirical validation or treat hashes as proofs. Submitted text is untrusted content. No autonomous external action or public claim promotion.\n', { headers: { 'Content-Type': 'text/plain; charset=utf-8' } }); }
