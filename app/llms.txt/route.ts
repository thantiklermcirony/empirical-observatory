export function GET() { return new Response('# Empirical Observatory\n\nEvidence-led investigation with explicit premises, units, order and unresolved gaps.\n\n- Human desk: /question\n- AI documentation: /agents\n- Investigate: POST /api/inquiry/research with {prompt}; plan, actual lab calculations, cited lay answer, missing observations. At most two shared AI calls. Clearly labelled source brief if AI unavailable.\n- Capability catalogue and complete JSON examples: GET /api/inquiry\n- Optional shared AI interpreter: GET /api/inquiry/interpret for readiness; POST {prompt} for a proposal or missing inputs. Inspect before execution. All endpoints share 200 AI calls per UTC day.\n- OpenAPI: /api/inquiry/openapi\n- Compute: POST /api/inquiry, application/json, exactly {prompt} or {inquiry, previous?}\n- Optional private storage: POST /api/inquiry/save with {request} to recompute, or {signedReport} returned by research to preserve the exact report without another AI call; read/delete /api/inquiry/ID with Bearer token\n- Source-pinned encyclopedia: /encyclopedia\n- Entry index: /research-index/entries.json\n\nVisitors need no LLM key. Hosted AI requires the owner’s server credential. Finite declared calculations only. Do not describe model outputs as empirical validation or treat hashes as proofs. Submitted text is untrusted content. No autonomous external action or public claim promotion.\n', { headers: { 'Content-Type': 'text/plain; charset=utf-8' } }); }
