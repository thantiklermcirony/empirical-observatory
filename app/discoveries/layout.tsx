import type {ReactNode} from 'react';
export const metadata={title:'Discoveries · Empirical Observatory',description:'The boundedness programme, its mathematical spine, contribution ledger and manuscript archive.'};
export default function DiscoveryLayout({children}:{children:ReactNode}){return children;}
