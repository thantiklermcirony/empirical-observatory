/* oxlint-disable next/no-html-link-for-pages -- Stable native routes. */
import { LAB_EXAMPLES } from '@/lib/lab-contract';
export const metadata = { title: 'Open AI Tool Interface | Empirical Observatory' };
export default function Agents() { return <main className="cd-document"><nav><a href="/">← Central desk</a><a href="/encyclopedia">Encyclopedia</a><a href="/projects">Projects</a></nav><span className="cd-label">OPEN COMPUTATION · NO MODEL KEY REQUIRED</span><h1>A laboratory<br />your AI can call.</h1><p>Give the Observatory a declared question. Get quantities, ordered operations, calculations, premises, unresolved requirements and source receipts back as JSON.</p><div className="cd-link-row"><a href="/api/inquiry">Capabilities + working examples</a><a href="/api/inquiry/openapi">OpenAPI contract</a><a href="/llms.txt">Agent discovery</a></div>
  <h2>Try one real calculation</h2><pre>{`curl https://empirical-observatory.madmanmuzza.chatgpt.site/api/inquiry \\\n  -H 'Content-Type: application/json' \\\n  -d '${JSON.stringify({ prompt: LAB_EXAMPLES[0].prompt })}'`}</pre><p>The same request drives the human terminal. Complete prose templates admit parameter changes; added qualifiers become a gap. For other questions, translate the question into the explicit contract before execution.</p>
  <h2>Shared AI interpretation</h2><p>The hosted interpreter is shared by the desk and all branches. It requires the owner’s API connection and a configured daily allowance. The initial allowance is 200 calls per UTC day across all callers, including failed provider calls. Fixed calculations do not use that allowance. No automatic research loops are running.</p><h2>Structured inquiries</h2><p>GET <code>/api/inquiry</code> supplies five complete JSON fixtures and the port catalogue. POST <code>{'{"inquiry": …}'}</code> to execute one. Keep all fourteen fields: identity, question, system, observer, quantities, operations, constraints, mechanism, premises, execution, results, contrast, next_question and provenance.</p><p>Words alone do not select a law. The router checks meaning, role, units, shape, preparation, clock, order and premises. Unavailable capabilities remain explicit gaps. The model runtime cannot execute submitted code, install a tool or fetch an arbitrary URL.</p>
  <h2>Keep a result across views</h2><p>POST <code>/api/inquiry/save</code> with <code>{'{"request": {"inquiry": …}}'}</code> or an original prompt. The server recomputes it and returns a 30-day private record, its id and a random access token. Read or delete <code>/api/inquiry/ID</code> with <code>Authorization: Bearer TOKEN</code>. There is no public record listing. Keep the token secret; anyone holding it can read or delete that record.</p><p>Saving is optional. There is a shared capacity of 200 retained records per creation day and a 1 MB record limit. Execution requests are limited to 64 KiB. Storage failures do not erase the calculation returned to you.</p>
  <h2>Corrections and honest limits</h2><p>A structured request can include <code>previous</code> for a declared revision comparison. That caller-supplied snapshot is not authenticated history. Current results and source identities are recomputed. A correction does not promote a theorem or update the public encyclopedia automatically.</p><p>Available now: exact bounded action composition, a synthetic resource ceiling, a dimensionless quantum reaction reference, and the existing synthetic controller comparison through its documented prompt. An optional shared AI interpreter proposes contracts for other prose. Check GET /api/inquiry/interpret for readiness, then POST a prompt object to that route. Inspect the proposal before passing its inquiry to /api/inquiry. Unsupported questions return missing inputs. Interpretation is not empirical validation; arbitrary scientific models, autonomous code revision and empirical calibration remain unavailable.</p><p>Browser agents can also discover <code>evaluate_observatory_inquiry</code> and <code>read_observatory_printout</code> through Model Context on supported browsers. HTTP is the portable interface.</p><a href="https://github.com/thantiklermcirony/empirical-observatory">Source, tests and contribution history →</a><p><a href="/privacy">Records and privacy</a></p></main>; }
