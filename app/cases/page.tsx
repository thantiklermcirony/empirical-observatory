/* oxlint-disable next/no-html-link-for-pages -- Native links share stable published routes. */
import {RESEARCH_CASES} from '@/lib/research-cases';
import '@/components/observatory/research-case-engine.css';
export default function Cases(){return <main className="rce"><div className="rce-index"><nav className="rce-nav"><a href="/">← Observatory</a><a href="/labs/biology">Biology</a><a href="/ledger">Scientific ledger</a></nav><header className="rce-header"><div><span className="rce-eyebrow">THE RESEARCH CASE ARCHIVE</span><h1>A question becomes<br/><em>an executable case.</em></h1><p>Preserve what was asked, what was calculated, what failed, and what observation could change the answer.</p></div></header><div className="rce-case-grid">{RESEARCH_CASES.map(c=><article key={c.id}><span className="rce-eyebrow">{c.id} / {c.classification.replaceAll('-',' ')}</span><h2>{c.title}</h2><p>{c.question}</p><p>{c.admission.replaceAll('_',' ')}</p><a href={c.href}>Open case & run checks →</a></article>)}</div><p>Execution and source publication do not admit a new law. Every case has its own model, observation boundaries and evidence gates.</p></div></main>;}
