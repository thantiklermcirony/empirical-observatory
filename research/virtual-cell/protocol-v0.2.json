{
  "version": "0.2",
  "frozen_before_matrix_values_inspected": true,
  "date": "2026-09-10",
  "task": "Four-context population log2-fold-change transfer, without held-out basal expression",
  "contexts": ["K562", "RPE1", "HepG2", "Jurkat"],
  "panels": "Annotation intersection of all available outcome-gene and target labels; report archive preprocessing and unsupported labels separately",
  "split": "Leave one whole context out; hyperparameters selected with inner leave-one-training-context-out folds",
  "scale": "Author-published log2FoldChange; preserve input values without renormalization; do not infer the estimator from a column name",
  "models": ["zero", "global_template", "target_mean", "global_shrink", "heterogeneity_shrink", "permuted_target_mean"],
  "global_template": "Mean across training contexts and available common targets, repeated for every target",
  "global_shrink_grid": [0, 0.25, 0.5, 0.75, 1],
  "heterogeneity_shrink_grid": [0, 0.25, 0.5, 1, 2],
  "heterogeneity_shrink": "For each target: mu=mean training effect; M=mean_gene(mu^2); V=mean_gene(sample variance between training contexts,ddof1); alpha=max(1-lambda*(V/n)/M,0), M=0 -> zero prediction. This is a heuristic positive-part shrinkage family, not a novel IDA theorem or calibrated Bayesian probability.",
  "tuning": "Choose lowest equal-context mean target-MSE; exact numerical ties choose smallest alpha for global shrink or largest lambda for heterogeneity shrink",
  "nonfinite_rule": "Fail ingestion and report counts; no zero imputation or selective dropping",
  "negative_control": "Independently permute target identities within each training context using seeded permutations; held-out truth unchanged. Pure context-label permutation is inert for these symmetric predictors.",
  "permutation_seed": 20260910,
  "risk_ranking": ["absolute training disagreement V", "relative disagreement V/(M+V), all-zero denominator ranks as 1", "training effect magnitude M (smallest retained first)", "mean of 50 random rankings with seed 20260911"],
  "risk_curves": "Retained fractions 0.1,0.2,...,1.0, target macro-MSE of target_mean and tuned heterogeneity_shrink; record exact retained target identities",
  "primary": "Target macro-MSE over every common outcome gene, reported per context and equally across contexts",
  "secondary": ["target-level MSE", "cosine direction similarity with zero norms explicitly undefined", "fraction of targets improved", "selected hyperparameters and inner losses", "risk-coverage curves"],
  "prospective_advantage_gate": "At least10% MSE improvement over the strongest eligible conventional comparator in at least3/4 outer contexts, no worse overall; engineering gate not significance. Negative findings remain results.",
  "scope_limits": ["Four selected cell lines and distinct protocols", "No individual-cell prediction or raw-count generation", "No external challenge score", "No held-out context features; cannot identify target-context-specific directions", "Known effects already estimated using each archive's original experimental controls and DE pipeline", "Dataset gene filtering precedes this study and may reflect perturbed outcomes", "No future modification of grids in response to outer results without a new labeled development study"]
}
