# Runtime ranges, not a frozen lockfile. Exact observed versions are recorded
# in reference-results/provenance.json and RELEASE_VALIDATION.json.
anndata>=0.10,<0.14
numpy>=1.26,<3
scipy>=1.11,<2
pandas>=2.1,<4
h5py>=3.10,<4
