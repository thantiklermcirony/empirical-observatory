'use client';
/* oxlint-disable next/no-html-link-for-pages -- Stable device identities survive room moves. */
import {useState} from 'react';
import {SCIENCE_ROOMS,DEVICES,available,roomForDevice,catalogueIssues} from '@/lib/observatory-catalogue';
export default function CatalogueInspector(){const [query,setQuery]=useState(''),[room,setRoom]=useState('all');const issues=catalogueIssues();const filtered=DEVICES.filter(d=>(room==='all'||roomForDevice(d.id)?.id===room)&&[d.label,d.kind,d.object,d.purpose,...(roomForDevice(d.id)?.disciplines??[])].join(' ').toLowerCase().includes(query.toLowerCase()));return <main className="catalogue-inspector"><nav><a href="/">← Night office</a><a href="/#deck">Room entrances</a><a href="/api/catalogue">Machine-readable catalogue ↗</a></nav><header><span className="office-kicker">THE OBSERVATORY / ORGANISATION</span><h1>Rooms can change.<br/>The evidence keeps its address.</h1><p>{SCIENCE_ROOMS.length} broad rooms · {DEVICES.length} instruments and planned devices. Disciplines are an expandable index, not a claim that every field has a working experiment.</p></header><div className="catalogue-filters"><label>Find a project, instrument or discipline<input value={query} onChange={e=>setQuery(e.target.value)} placeholder="Try EEG, quantum, genetics, adaptation…"/></label><label>Room<select value={room} onChange={e=>setRoom(e.target.value)}><option value="all">Every room</option>{SCIENCE_ROOMS.map(r=><option key={r.id} value={r.id}>{r.name}</option>)}</select></label></div><div className="catalogue-table-wrap"><table><thead><tr><th>Instrument</th><th>Home & connections</th><th>What it actually does</th></tr></thead><tbody>{filtered.map(d=><tr key={d.id}><td><a href={`/devices/${d.id}`}>{d.label} ↗</a><small>{available(d)?'Available instrument':'Under construction'}</small><code>{d.id}</code></td><td><a href={`/labs/${roomForDevice(d.id)!.id}`}>{roomForDevice(d.id)!.name}</a><div>{d.related.map(id=><a key={id} href={`/devices/${id}`}>{DEVICES.find(x=>x.id===id)!.label}</a>)}</div></td><td><strong>{d.kind}</strong><p>{d.purpose}</p><a href={`https://github.com/thantiklermcirony/empirical-observatory/blob/main/${d.source}`}>Source ↗</a></td></tr>)}</tbody></table>{!filtered.length&&<p>No matching instrument. Clear the search or choose another room.</p>}</div><section className="catalogue-integrity"><h2>How this organisation adapts</h2><p>{issues.length?issues.join('; '):'Catalogue checks pass: each device has one home, every cross-reference resolves, and room merges contain no cycles.'}</p><p>The catalogue recommends keeping one home per instrument and cross-linking other disciplines. Split an overcrowded room around a distinct scientific task; merge an empty room by redirecting its identity. A move never changes the engine or promotes its results.</p><ul><li>Chemistry and Astronomy need their first admitted instruments. Their rooms link to related work and state what is missing.</li><li>IDA belongs in Biology, with connections to Psychology and the resource ledger. Its EEG is local; it is not silently supplied to hosted AI.</li><li>The central computer uses its reviewed computation contracts. A room link alone does not make a browser instrument executable by AI.</li></ul><a href="/research/Observatory_Room_Architecture.md">Read the extension and admission contract ↗</a></section></main>;}
